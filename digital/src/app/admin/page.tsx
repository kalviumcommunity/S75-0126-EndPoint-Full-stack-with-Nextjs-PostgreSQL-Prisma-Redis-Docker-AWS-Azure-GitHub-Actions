// src/app/admin/page.tsx

import { ReactElement } from "react";

// Next.js expects a default export function component
export default function AdminPage(): ReactElement {
  return (
    <div>
      <h1>Admin Page</h1>
      <p>This is a placeholder page for the admin route.</p>
    </div>
  );
}
